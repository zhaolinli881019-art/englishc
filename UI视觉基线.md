# 英语单词 App：UI 视觉基线

记录日期：2026-08-12

## 当前决定

以下 6 个界面是用户当前喜欢并要求暂时保留的视觉方案。后续讨论按钮、交互、文案、数据规则和页面跳转时，以这 6 个界面为基线，不擅自改变整体视觉方向。

## 统一视觉风格

- iPhone 竖屏界面。
- 暖白色、带轻微纸张质感的背景。
- 大面积留白，界面轻盈、安静。
- 所有系统界面文案使用英文。
- 字体采用可爱、圆润、手写感的英文字体，同时保证清晰易读。
- 主要颜色为黑色、浅灰、薄荷绿、柔和黄色、珊瑚红和淡紫色。
- 《空洞骑士：丝之歌》人物作为陪伴角色；当前示例多使用 Hornet。
- 用户在角色选择页选择的人物，应继续出现在答题页、结果页和学习日记页。
- 当前先用于产品原型；如正式商业发布，需要重新评估游戏人物的版权授权。

## 保留的 6 个界面

### 1. 首页

- 左侧模式选择：`Dictation`、`C → E`、`E → C`。
- 中间显示年份、月份和紧凑日历。
- 已有记录的日期由人物贴纸覆盖日期位置。
- 日历人物相较当前稿放大约 20%～25%，但不得侵占相邻日期点击区域。
- 底部显示 `Import Word List` 横向卡片。
- 右上角保留设置入口。
- 右上角设置图标当前只作为预留入口；设置页及后台功能后期再设计和实现，本阶段不展开。
- 点击中间的年份/月标题（`2026 / AUGUST`）打开年份与月份选择弹层；弹层采用居中的白色圆角面板、上一年/下一年箭头和 12 个月网格。
- 点击普通日期查看当天记录；年份/月标题负责切换月份，两种点击行为分开。

参考图：`codex-clipboard-006b2d37-1630-4865-b672-5442a1427308.png`

### 2. 导入确认页

- 页面标题：`Confirm Import`。
- 顶部使用 Hornet 人物。
- 显示导入总数，如 `50 Words Found`。
- 大量未填写日期的单词在本页选择 `All Today` 或 `Split by Day`，不新增页面。
- 选择 `Split by Day` 后，可在同一区域设置每天单词数和开始日期，如 `10 words / day`、`Starting Aug 12`。
- 日期较多时只展示部分日期，其余使用 `More Dates` 汇总。
- 错误日期使用 `Dates Need Review` 提示。
- `Word Preview` 固定展示 3 个示例，不展示全部单词，也不显示 `View all words`。
- 底部按钮根据数量显示，如 `Import 50 Words`。

参考图：`codex-clipboard-cbb90d7e-d79e-4461-a1be-fc0cb77c325e.png`

### 3. 角色选择页

- 标题：`Choose your character`。
- 角色采用 3 列宽松排列，最后一行居中。
- 不显示角色名称，不使用卡片边框。
- 选中角色使用浅色圆形底和小勾表示。
- 底部保留继续箭头。

参考图：`codex-clipboard-0785a2fc-6030-4462-82d8-6f79c906afc4.png`

### 4. 答题页

- 首页已经选择模式，因此答题页不重复显示模式选择。
- 三种模式共用一个答题模板：
  - `Dictation`：播放发音，输入英文。
  - `C → E`：显示中文，输入英文。
  - `E → C`：显示英文，输入中文。
- 顶部显示答题进度。
- 显示第三页选中的人物。
- 保留重播、`I don't know` 和 `Submit`；输入区域采用无外框的虚线底线样式。
- 根据模式显示英文或中文键盘。

参考图：`codex-clipboard-6358f707-7a36-43cd-9891-8b1a770c2e10.png`

### 5. 本次详细结果页

- 标题根据模式变化，如 `Dictation Result`、`C → E Result`、`E → C Result`。
- 显示总题数、正确数、错误数、未答数和正确率。
- 列出本次测试的全部单词。
- 正确题显示 `Correct`。
- 错题同时显示孩子原答案和正确答案；错误答案使用珊瑚红，正确答案使用薄荷绿。
- 未答题显示 `—` 以及正确答案。
- 显示本次使用的人物。
- 有错题或未作答题时，底部显示 `Practice N Again`，点击后沿用当前模式和人物复测这些题。
- 同时保留次要操作 `Done`；全部正确时只显示 `Done`。
- 复测不覆盖第一次成绩，作为同一次学习记录中的后续练习保存。

参考图：`codex-clipboard-8a36fd4e-fae5-40b4-8213-2a9a9c202e04.png`

### 6. 按日学习日记页

- 中间显示年份，右上角保留搜索。
- 测试记录按日期持续保存。
- 每条记录显示当日人物、日期、星期和模式。
- 系统自动把测试结果整理成自然语言段落，不需要用户手动填写。
- 段落包括测试过的单词、正确/错误/未答数量、正确率和错误拼写。
- 错误拼写使用珊瑚红突出。
- 点击某天记录，进入该次详细结果页。

参考图：`codex-clipboard-7bdc603d-822b-468d-8824-67d7fef6ff52.png`

## 后续待确定

- 每个按钮的准确位置、尺寸、点击反馈和禁用状态。
- 六个界面之间的跳转关系及返回逻辑。
- 首页点击日期后的行为。
- 导入日期错误的修改方式。
- 角色是否每次测试选择，还是保存默认角色。
- 答题后是否即时显示对错。
- 结果页长列表的滚动与筛选方式。
- 同一天进行多次测试时，学习日记如何展示。
- 搜索、设置和数据保存细节。
- 设置后台（账号、备份、发音、判分规则等）统一放到后期处理。
